spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: ''
        url: jdbc:mysql://localhost:3306/konacafe?autoReconnect=True$serverTimezone=UTC&useSSL=false
        username: root
    jpa:
        database: mysql
        show_sql: true
